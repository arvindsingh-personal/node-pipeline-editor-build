# node-pipeline-editor-build
This project is a 'node-based pipeline builder' built using React (React Flow) and FastAPI.  Users can create and connect different types of nodes to form a pipeline, submit it, and receive analysis about the pipeline structure from the backend.
